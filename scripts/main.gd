extends Node

# Main currently starts directly in the settlement management view for the MVP.
# Later this scene can host the normal player world and open that view on demand.

func _ready() -> void:
	print("Hex Survival MVP gestartet")
